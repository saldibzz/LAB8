import { Component } from '@angular/core';

@Component({
    selector: 'albums',
    templateUrl: `./albums.component.html`
})
export class AlbumsComponent {
    name = "Albums!"
}

interface Albums {

}